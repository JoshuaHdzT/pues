<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.login">

    <uses-permission android:name="android.permission.INTERNET" /> <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Restaurant"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditarPerfil"
            android:label="Editar Perfil"></activity>
        <activity
            android:name=".Valorar_servicio"
            android:label="Valore el Servicio" />
        <activity
            android:name=".EvaluarComida"
            android:label="Ingrese la evaluacion de la comida" />
        <activity
            android:name=".Pago"
            android:label="Datos del pago" />
        <activity
            android:name=".Historial"
            android:label="Historial de Compras" />
        <activity
            android:name=".Principal"
            android:label="Platillos" />
        <activity
            android:name=".Registro_usuario"
            android:label="Registro" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>